// Root layout - DO NOT add <html> or <body> tags here
// Those are added in [locale]/layout.tsx

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children;
}
